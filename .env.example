PORT=3001
NODE_ENV=development

# Leave DATABASE_URL blank locally — the backend uses SQLite automatically.
# On Render, this is injected from the free Postgres database; you never paste it manually.
# DATABASE_URL=
