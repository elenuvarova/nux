{
  "name": "nux-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:tokens": "node scripts/sync-tokens.mjs",
    "build:films": "node scripts/build-landing-films.mjs",
    "generate": "npm run sync:tokens && npm run build:films",
    "predev": "npm run generate",
    "prebuild": "npm run generate",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4173"
  },
  "dependencies": {
    "@microsoft/clarity": "^1.0.2",
    "react": "^18.3.1",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
